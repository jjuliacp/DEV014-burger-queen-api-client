import Layout from "./Layout";

const WaiterView: React.FC = () => {
  return (
    <Layout>
      <div>
        {/* Contenido de la vista del mesero */}
        <h1>Vista del Mesero</h1>
      </div>
    </Layout>
  );
};

export default WaiterView;
